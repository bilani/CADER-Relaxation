# RUN THE APP USING : 

### mvn exec:java -Dexec.mainClass="org.apache.jena.sparql.Education"

(Query using SPARQL, relying on RDF)

Query relaxation, relying on calculating MFS and XSS for a given SPARQL Query in order to find the query's unresolved constraints
